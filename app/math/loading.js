// Shown instantly while the session check + RSC payload for /math load,
// so switching over from /japanese never sits on a blank screen.
export default function Loading() {
  return <div className="tk-loading-screen">Loading…</div>;
}
